<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- GPS capture on frame log (Quick Screen) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Haptic feedback via direct VibrationEffect — required for stepper and Log Frame haptics -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Detent.Starting">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="DETENT"
            android:theme="@style/Theme.Detent.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                Deep link used by the widget's Roll Complete button to navigate directly
                to the Roll Journal for the active roll. URI format: detent://journal/{rollId}
                Matched by the RollJournal composable in NavGraph via navDeepLink.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="detent" android:host="journal" />
            </intent-filter>
        </activity>

        <!--
            OSS Licenses activities — must use an AppCompat theme because they extend
            AppCompatActivity. Theme.Detent.OssLicenses is an AppCompat-compatible theme
            that matches the app's Golden Hour color palette.
        -->
        <activity
            android:name="com.google.android.gms.oss.licenses.v2.OssLicensesMenuActivity"
            tools:replace="android:theme"
            android:theme="@style/Theme.Detent.OssLicenses" />
        <activity
            android:name="com.google.android.gms.oss.licenses.v2.OssLicensesActivity"
            tools:replace="android:theme"
            android:theme="@style/Theme.Detent.OssLicenses" />

        <!--
            FileProvider exposes backup files from the cache directory via content:// URIs
            so the system share sheet can read them. Authority must match the string used
            in SettingsScreen.kt when calling FileProvider.getUriForFile().
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!--
            App Widget receiver. The APPWIDGET_UPDATE intent is required for the launcher
            to find and add the widget. The meta-data block points to the widget's
            AppWidgetProviderInfo XML (size, update period, initial layout, etc.).
        -->
        <receiver
            android:name=".ui.widget.FrameLogWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/frame_log_widget_info" />
        </receiver>

    </application>

</manifest>